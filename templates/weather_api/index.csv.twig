city,country,date,celsius
{%- for measurement in measurements %}
    {{ city }},{{ country }},{{ measurement.date|date("Y-m-d") }},{{ measurement.temperature }},{{ measurement.getFahrenheit }}
    {%- if not loop.last %},{% endif %}
{% if not loop.last %}\n{% endif -%}
{%- endfor %}